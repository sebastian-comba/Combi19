<%- include("partials/header-chofer"); -%>
  <h1>Registrar síntomas</h1>
  <h5>Email del comprador: <%= data.emailPasajero%>

  </h5>
  <h5>Pasajes Comprados: <%= data.cantidad%>
  </h5>
  <% if (!data) { %>
    <h2>Error</h2>
    <% } else { %>
      <br> 
      <% for (i=1;i<=data.cantidad;i++){%>
        <div class="card">
          <div class="card-body">
            <h5 class="card-title">Pasaje <%= i %>
            </h5>
            <class class="card-text">
              <form id="form_<%=i%>" >
                <label for="temperatura">Temperatura:</label>
                <input name="temperatura" class="sintoma" step="0.1" name="form_<%=i%>" required type="number"><br><br>
                <label >Sintomas que tubo en los ultimos 15 dias <small>(debe marcar las que tubo)</small></label> <br>
                  <input type="checkbox"  name="form_<%=i%>"  >
                  <label for="sintoma1"> Fiebre  </label> 
                  <br>
                  <input type="checkbox"  name="form_<%=i%>" >
                  <label for="sintoma2"> Perdida gusto y/o olfato en la ultima semana</label> 
                  <br>
                    <input type="checkbox" name="form_<%=i%>"  >
                    <label for="sintoma3"> Dolor garganta</label>
                  <br>
                    <input type="checkbox" name="form_<%=i%>"  >
                    <label for="sintoma4"> Dificultad respiratoria</label>
                  
              </form>

            </class>
          </div>
        </div>
        <br>
        <% } %>
          <div id="err"></div>
          <button id="validar">Verificar sintomas</button>
          <a href="http://localhost:3000/listar-pasajeros/<%= data.idViaje %>">
            <button id="cancelar">Cancelar</button>
        </a>
          <% } %>
        <input type="hidden" name="idViaje" id="idViaje" value="<%= data.idViaje %>">
        <input type="hidden" name="idPasaje" id="idPasaje" value="<%= data._id %>">
        <input type="hidden" name="emailPasajero" id="emailPasajero" value="<%= data.emailPasajero %>">
        <input type="hidden" name="cantidadAsientos" id="cantidadAsientos" value="<%= data.cantidad %>">
        <script src="/js/registrar-sintomas.js">
        </script>
            <%- include("partials/footer"); -%>